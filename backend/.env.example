# Copy to .env for local (non-Docker) development. Docker Compose sets these directly
# via environment: in docker-compose.yml instead of reading this file.
DATABASE_URL=sqlite:///./dev.db
CORS_ORIGINS=
